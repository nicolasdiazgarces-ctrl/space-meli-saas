# ── Supabase ─────────────────────────────────────────────────────────────────
# Obtener en: app.supabase.com → tu proyecto → Settings → API
SUPABASE_URL=https://XXXXXXXXXX.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5...

# ── Anthropic (Claude) ────────────────────────────────────────────────────────
# Obtener en: platform.claude.com → Settings → API Keys
# NUNCA poner esta key en el frontend — va SOLO en Vercel env vars
ANTHROPIC_API_KEY=sk-ant-api03-...

# ── Estas variables van en Vercel Dashboard → Settings → Environment Variables ──
# NO subir este archivo con los valores reales a GitHub (el .env.example con placeholders sí está ok)
